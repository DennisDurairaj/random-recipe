{
  "name": "random-recipe-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "dev": "nodemon src/server.ts --inspect",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/server.js",
    "build:prod": "tsc",
    "start:prod": "npm run build:prod && node build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.12",
    "ts-node": "^10.2.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/yamljs": "^0.2.31",
    "@types/axios": "^0.14.0",
    "@types/express": "^4.17.13",
    "@types/morgan": "^1.9.3",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
